"use client";

import { ProductsType } from "@/type";


export const ProductCard = ({ product }: { product: ProductsType }) => {
    return (
        <div>
            ProductCard
        </div>
    )
}
